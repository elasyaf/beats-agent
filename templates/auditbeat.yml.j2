auditbeat.modules:

- module: audit
  metricsets: [file]
  file.paths:
  - /bin
  - /usr/bin
  - /sbin
  - /usr/sbin
  - /etc

output.elasticsearch:
  hosts: ["{{elasticsearch_hosts}}:9200"]

setup.kibana:
  host: "{{kibana_hosts}}:5601"
